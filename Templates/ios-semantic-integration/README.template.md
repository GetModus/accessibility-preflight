# Semantic Integration Review for {{APP_SLUG}}

> Warning: proposed and not applied.
>
> No app code was changed automatically.

This bundle was generated as a standalone review artifact under `{{ARTIFACT_DIR}}`.
Review the proposed Swift export before copying anything into the app source tree.

## Review

- Project root: `{{PROJECT_ROOT}}`
- App source root: `{{APP_ROOT}}`
- Proposed export: `{{EXPORT_FILENAME}}`

## Apply After Approval

1. Review the generated Swift file and the surrounding guidance.
2. Approve the change with the app owner or reviewer.
3. review and apply after approval.
4. Re-run the preflight after adoption to confirm the semantic integration is live.
